# DinkAndLadder Mobile (Placeholder)

Flutter is the planned mobile client for DinkAndLadder (see `/docs/PLAN.md` and `/docs/17-UI-UX-ARCHITECTURE.md`).

This directory is a placeholder. The Flutter project has not been initialized yet.

Mobile app work is scoped to Phase 6 (Mobile Expansion) in `/docs/10-IMPLEMENTATION-BACKLOG.md`, after the MVP web/API surface is built. Until then:

- The mobile client will consume the same `/api/v1` contracts defined in `/docs/06-API-CONTRACTS.md`.
- DTOs and API design must remain mobile-compatible from the start, per `/docs/01-AI-CONSTITUTION.md` (Rule 7).
- Do not add Flutter source code here until the backlog promotes mobile work into scope.
