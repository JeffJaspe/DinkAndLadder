changelog-file=db.changelog-master.xml

# Connection URL, username, and password are intentionally NOT set here.
# Liquibase reads them from environment variables at run time so no credentials
# are ever committed to the repository:
#   LIQUIBASE_COMMAND_URL
#   LIQUIBASE_COMMAND_USERNAME
#   LIQUIBASE_COMMAND_PASSWORD
#
# See database/liquibase/README.md for local setup and database/.env.example
# for the variable names used to populate them.
