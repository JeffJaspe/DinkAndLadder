# Copy to database/.env (gitignored) and fill in local/dev values.
# Never commit real credentials.

# Direct PostgreSQL connection (Supabase project or local Postgres).
LIQUIBASE_COMMAND_URL=jdbc:postgresql://localhost:5432/dinkandladder
LIQUIBASE_COMMAND_USERNAME=postgres
LIQUIBASE_COMMAND_PASSWORD=changeme
